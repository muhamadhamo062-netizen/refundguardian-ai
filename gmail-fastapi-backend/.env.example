# 32-byte url-safe base64 key for Fernet (generate with:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

# OpenAI API key for complaint letter generation
OPENAI_API_KEY=

# Optional: path to encrypted credentials store (JSON)
CREDENTIALS_STORE_PATH=./data/encrypted_credentials.json
